services:
  - type: web
    name: noir-devops-tools
    runtime: python
    buildCommand: pip install -r tools/api/requirements.txt
    startCommand: uvicorn tools.api.app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health